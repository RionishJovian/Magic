# Recipient is safe to commit. Configure a real SMTP sender separately in
# /etc/mikromagic-monitor.env with mode 0600; never commit its password.
ALERT_TO=rionishjovian1999@gmail.com
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURITY=starttls
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
